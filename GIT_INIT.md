# Git Init Checklist

```bash
cd /Users/ian/.codex/skills/video-auto-snapshot
git init
git add .
git commit -m "$(cat COMMIT_MESSAGE.txt)"
```

Optional next steps:

1. Create a GitHub repository
2. Add a remote
3. Push the initial commit
